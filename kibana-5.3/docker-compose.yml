version: '3.1'
services:
  kibana:
    image: 'fxdgear/kibana:5.3'
    build:
      context: ../
      dockerfile: kibana-5.3/Dockerfile
    networks:
      - esnet
    ports:
        - '5601:5601'
    deploy:
      mode: 'replicated'
      replicas: 1
    depends_on:
      - elasticsearch

volumes:
  esdata:
    driver: local
networks:
  esnet:
