version: '3.1'
services:
  elasticsearch:
    image: 'fxdgear/elasticsearch:5.3'
    build:
      context: ../
      dockerfile: elasticsearch-5.3/Dockerfile
    environment:
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - "ES_HOST=http://elasticsearch:9200"
      - cluster.name=docker-cluster
      - bootstrap.memory_lock=true
    volumes:
      - esdata:/usr/share/elasticsearch/data
    networks:
      - esnet
    ports:
        - '9200:9200'
    deploy:
      mode: 'replicated'
      replicas: 1
      resources:
        limits:
          memory: 1g
        ulimits:
          memlock:
            soft: -1
            hard: -1

volumes:
  esdata:
    driver: local
networks:
  esnet:
